# RealAgent

**Real estate listing management system — being rewritten in Go.**

Under active development. Original Python implementation archived under `python/`.

## Status

Full Go rewrite in progress. Go binary replaces the Python stack entirely (scraping, dashboard, API).

```sh
go build -o realagent .
./realagent
```

Opens dashboard at `http://localhost:8080`.
