*) Add more details to the cards in list view (or delete it completely idk) ???
*) In all the drowdown menus, make sure that the arrow doesnt have so much padding (make it like on the main screen)
*) In the modals all the text must be identical (bold where needed)
*) The journal need more details about what exactly changed for that listing
*) Add some measurements in the cli about the performance of the db
*) Think about protecting the db access, login pannel on start with token active for 4hr's
*) Add a flashy RealAgent lotti loggo on the Login Screen, before presenting the login/pass input fields
*) Make better use of width, don't make everything too narrow
*) How do we package everything so it makes sense as a SaaS , with invalidation and keys if the clients didn't pay for this month.
*) Hosting for the pwa listings should happen automatically (maybe free options) ?
*) Real Agent will be hosted locally (and only listings pwa cloud), or Real Agent VPS (need to think this good)
* Clarify with the PWA infrastructure, if it works as needed.
*) Need to be able to push notifications to the users that accept notifications for a specific listing. Updated the price - let the user automatically (with a message to the Agent).



* Separate the logic inside the agent.py (for the 999.md, in case we will add more websites logic that we will need to implement in the future.) Keep them organized. (maybe use AI to read the page, mark the data and create a query to the db directly ? HZ